{
  "protocol_version": "mnq-depth-research-v1",
  "registered_at": "2026-09-22",
  "parent_protocol": "mnq-probabilistic-research-v1",
  "purpose": "Incremental value of depth, research and Dry Run only; no order routing or automatic promotion",
  "minimum_total_sessions": 200,
  "training_sessions": 60,
  "calibration_sessions": 20,
  "validation_sessions": 20,
  "walk_forward_step_sessions": 20,
  "minimum_walk_forward_folds": 3,
  "untouched_final_sessions": 60,
  "minimum_training_examples": 300,
  "minimum_effective_days_per_forecast": 20,
  "minimum_validation_trades": 200,
  "minimum_final_trades": 200,
  "minimum_forward_dry_run_sessions": 20,
  "horizons_seconds": [1, 5, 30, 900],
  "primary_horizon_seconds": 900,
  "short_horizons_are_diagnostics_only": true,
  "decision_grid_seconds": 300,
  "feature_window_seconds": 30,
  "depth_levels": 5,
  "maximum_feed_age_seconds": 2,
  "maximum_provider_lag_seconds": 2,
  "feature_sets": ["candles", "level1", "level2", "level2_no_shape", "level2_no_flow"],
  "candidate": "depth_logistic_ridge_v1",
  "ridge_penalty": 1.0,
  "tuning_configurations": 1,
  "calibration": "none; experimental probabilities remain uncalibrated; normalization and payoff conditional means fit on training only",
  "comparison_family_size": 20,
  "commission_per_side_usd": 0.61,
  "quantity": 1,
  "entry_delay_seconds": 1,
  "slippage_ticks_per_side": 1,
  "label_execution": "Sampled one-second executable quotes, stop-first on gaps, marketable target exit, 900 seconds from decision. These are quote proxies, not actual fills or subsecond extrema.",
  "paired_session_coverage_fraction": 0.99,
  "paired_session_seconds": 22500,
  "candidate_filter_bases": ["bayesian_cells_v1", "kernel_paths_v1"],
  "minimum_net_edge_usd": 1.0,
  "uncertainty_z": 2.576,
  "cost_uncertainty_usd": 1.0,
  "max_daily_loss_usd": 250.0,
  "max_entries_per_day": 30,
  "cooldown_seconds": 300,
  "bootstrap_seed": 20260922,
  "bootstrap_replicates": 2000,
  "bootstrap_day_block_lengths": [5, 10],
  "familywise_alpha": 0.05,
  "acceptance": {
    "positive_paired_brier_improvement_lower_bound_vs_level1": true,
    "log_loss_no_worse_than_level1": true,
    "positive_net_expectancy_lower_bound": true,
    "positive_paired_daily_net_improvement_lower_bound_vs_level1": true,
    "execution_positive_paired_absolute_error_improvement_lower_bound_vs_level1": true,
    "maximum_absolute_calibration_error": 0.05,
    "minimum_observations_per_reliability_bin": 30,
    "maximum_drawdown_usd": 1000.0,
    "maximum_expected_shortfall_95_usd": 100.0,
    "maximum_positive_pnl_share_best_day": 0.2,
    "positive_net_after_removing_best_five_days_and_trades": true,
    "all_regimes_at_least_30_trades_and_nonnegative_mean": true,
    "all_stresses_nonnegative_net": true,
    "verified_unseen_holdout": true,
    "verified_feed_semantics": true,
    "verified_actual_execution_and_intratrade_drawdown": true,
    "parent_acceptance_still_required": true
  },
  "stresses": {
    "receive_delay_ms": [250, 1000, 3000],
    "drop_every_nth_event": [20],
    "stale_seconds": [3],
    "depth_levels": [3, 10],
    "extra_round_trip_cost_usd": [1.0, 3.0],
    "ridge_penalty": [0.5, 2.0]
  },
  "holdout_policy": "Reserve 60 complete paired sessions before constructing development labels. Never inspect final outcomes or use previously examined data as unseen. New parameters require a new protocol. No CLI final-test or promotion shortcut.",
  "missing_policy": "Abstain; no zero imputation, stale-book reuse, or unvalidated fallback. Invalid labels excluded and counted. Fixed matched cohort for all baselines and ablations.",
  "execution_policy": "Marketable displayed-book sweep is an instantaneous lower-bound scenario, not a promised fill. No passive fills from touching a price. Actual fill uncertainty remains unverified without matched own-fill observations."
}
